import java.util.*;

public class Book {
	public class bookDetail {
		String id;
		String title;
		String author;
		int issuedYear;
		
		public bookDetail(String newId, String newTitle, String newAuthor, int newIssuedYear) {
			id=newId;
			title=newTitle;
			author=newAuthor;
			issuedYear=newIssuedYear;
		}
	}
	
	List<bookDetail> books = new ArrayList<bookDetail>();
	
	books.add(new bookDetail("1", "자바 기초", "Jane", 2021));
	public void addBook(bookDetail newBook) {
		for(bookDetail book:books) {
			if(book.id==newBook.id) {
				throw new IllegalArgumentException("해당 ID("+newBook.id+")는 이미 존재합니다.");
			}
		}
		
		books.
	}
}
